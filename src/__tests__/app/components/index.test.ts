import MatrixDisplay from '@/app/components/MatrixDisplay';
import MatrixInput from '@/app/components/MatrixInput';
import MatrixSizeSelector from '@/app/components/MatrixSizeSelector';
import RotateButton from '@/app/components/RotateButton';

describe('MatrixDisplay', () => {
    it('should be defined', () => {
        expect(MatrixDisplay).toBeDefined();
    });
});

describe('MatrixInput', () => {
    it('should be defined', () => {
        expect(MatrixInput).toBeDefined();
    });
});

describe('MatrixSizeSelector', () => {
    it('should be defined', () => {
        expect(MatrixSizeSelector).toBeDefined();
    });
});

describe('RotateButton', () => {
    it('should be defined', () => {
        expect(RotateButton).toBeDefined();
    });
});
